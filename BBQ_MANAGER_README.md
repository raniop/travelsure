# מנהל על האש - BBQ Manager

מערכת לניהול תשלומים לחבורה שעושה "על האש" כל שבוע.

## תכונות

✅ **ניהול קבוצה** - רשימת חברים קבועה (10 חברים)
✅ **יצירת אירועים** - כל שבוע נוצר אירוע חדש עם עלות כוללת
✅ **סימון הגעה** - מי הגיע ומי לא
✅ **ניהול אורחים** - הוספת אורחים עם כלל "פעם ראשונה חינם, מפעם שנייה משלם"
✅ **חישוב אוטומטי** - חלוקת עלויות בין המשתתפים המשלמים בלבד
✅ **ניהול תשלומים** - מעקב אחר מי שילם ומי לא
✅ **היסטוריה** - צפייה בכל האירועים והתשלומים
✅ **תשלום דיגיטלי** - לינק תשלום (PayBox/Bit) לכל תשלום

## התקנה

### 1. יצירת טבלאות ב-Supabase

הרץ את קובץ ה-migration ב-Supabase:

```sql
-- קובץ: supabase/migrations/20240101000000_create_bbq_tables.sql
```

או העתק את התוכן והרץ אותו ב-Supabase SQL Editor.

### 2. הגדרת משתני סביבה

ודא שיש לך את המשתנים הבאים ב-`.env`:

```
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_PUBLISHABLE_KEY=your_supabase_key
```

### 3. הרצת הפרויקט

```bash
npm install
npm run dev
```

### 4. גישה למערכת

פתח בדפדפן: `http://localhost:5173/bbq`

## שימוש

### יצירת קבוצה ראשונה

בפעם הראשונה שתיכנס, המערכת תיצור אוטומטית קבוצה בשם "העל האש שלנו".

### הוספת חברים

1. לך לטאב "חברים"
2. לחץ על "הוסף חבר"
3. מלא שם, טלפון (אופציונלי), אימייל (אופציונלי)

### יצירת אירוע

1. לך לטאב "אירועים"
2. לחץ על "אירוע חדש"
3. בחר תאריך, הזן עלות כוללת, והוסף תיאור (אופציונלי)

### ניהול אירוע

1. לחץ על "פרטים" באירוע
2. סמן מי הגיע (חברים קבועים)
3. הוסף אורחים:
   - הזן שם אורח
   - טלפון (אופציונלי, אבל מומלץ לזיהוי)
   - המערכת תזהה אוטומטית אם זה ביקור ראשון (חינם) או שני+ (משלם)
4. לחץ על "חשב תשלומים" - המערכת תחלק את העלות בין כל המשתתפים המשלמים

### תשלומים

1. לך לטאב "תשלומים"
2. תראה רשימה של כל התשלומים
3. לחץ על "שלם עכשיו" כדי לפתוח לינק תשלום (PayBox/Bit)
4. אחרי התשלום, לחץ על "סמן כמשולם"

### היסטוריה

בטאב "היסטוריה" תוכל לראות את כל האירועים הקודמים.

## הגדרות תשלום

כרגע המערכת משתמשת בלינק תשלום דינמי. כדי להתאים את זה לספק התשלום שלך:

1. פתח את `src/components/bbq/PaymentsOverview.tsx`
2. מצא את הפונקציה `onClick` של כפתור "שלם עכשיו"
3. שנה את ה-URL לפי הספק שלך:
   - **PayBox**: `https://payboxapp.co.il/pay?amount=...`
   - **Bit**: `https://bit.co.il/pay?amount=...`
   - או כל ספק אחר

## מבנה הטבלאות

- `groups` - קבוצות
- `members` - חברים קבועים
- `events` - אירועים
- `attendees` - מי הגיע (חברים)
- `guests` - אורחים
- `payments` - תשלומים

## כללי אורחים

המערכת מטפלת אוטומטית בכלל "פעם ראשונה חינם, מפעם שנייה משלם":
- אורח מזוהה לפי טלפון
- ביקור ראשון: `should_pay = false`
- ביקור שני ומעלה: `should_pay = true`

## תמיכה

אם יש בעיות או שאלות, בדוק:
1. שהטבלאות נוצרו ב-Supabase
2. שהמשתנים ב-`.env` נכונים
3. את הקונסול בדפדפן לשגיאות

## שיפורים עתידיים

- [ ] אימות משתמשים (כל אחד נכנס עם משתמש משלו)
- [ ] סליקה מלאה בתוך האתר (PayMe/Tranzila)
- [ ] התראות פוש
- [ ] דוחות חודשיים
- [ ] ייצוא לאקסל
